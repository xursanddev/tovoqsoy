{
  "name": "tovoqsoy",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite build && vite preview --port 8080"
  },
  "devDependencies": {
    "vite": "^5.0.8"
  },
  "dependencies": {
    "halfmoon": "^2.0.1"
  }
}
